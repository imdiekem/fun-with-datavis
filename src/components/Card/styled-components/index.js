import styled from 'styled-components';

export const StyledCard = styled.div`
    padding: 40px;
    background: #fff;
    display: flex;
    box-shadow: 0 1px 2px 0 rgba(0,0,0,0.04), 0 1px 2px 0 rgba(0,0,0,0.04);
    font-size: 1rem;
`;